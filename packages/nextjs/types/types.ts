export type HeaderMenuLink = {
  label: string;
  key: string;
  icon?: React.ReactNode;
};

export interface Course {
  id: number;
  name: string;
}

export interface NFT {
  id: number;
  name: string;
}
